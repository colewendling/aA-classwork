export const allTodos = (state) => { //state: state
    return (
        // Object.keys(todos).map( id =>
        //     todos[id]    
        // );
        Object.values(state.todos)
            //todos[id]
        
    )
};